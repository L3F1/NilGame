# Fresh-chat handoff for Astra

2026-09-11. Read WORKING_RULES, inspect status, then relevant TASK_ROUTER row.
Global-host/MUSE59 batch accepted; latest MUSE60 portal-approach checks at52b70fd also accepted after lead report corrections.
No jobs running. Manifest names completed work: replace before dispatch. Claude's
only unexpected path was the lead-supplied contract after staging denial; known,
reviewed and documented. No retries/heartbeat prompts.

Delivered: Worlds -> Full S3 portals -> tools/connected-global-preview.html.
GPU + CPU-body E3 -> COMPLETE S3 past antipode -> E3, saved multi-chart fixture.
From flat spawn hold W about10 seconds. Global subset R8 additive balls, no
floor/gravity or editor controls. Shared connected renderer owns coverage-tagged
packets, global future aperture roots and64-unit rays; old bounded preview32.
Read CONNECTED_GLOBAL_GPU.md, COVER_REGION_FORMAT.md, and latest review
at docs/qa/connected-global-gpu-review.md. Scene-v2 chart limits unchanged.

Critical discovered trap: native GLSL sin/cos on SwiftShader caused .045-unit
hit error for a small spherical ball after a portal. Range-reduced polynomials
fixed it under unchanged .001 tolerance. NVIDIA and SwiftShader 24,000-ray view
corpora pass;11 extra hit refusals remain. Do not replace custom trig with native
calls for speed without rerunning software and hardware parity. GPU medians
.168-.182ms at320x240 on5070Ti (not end-to-end latency). See report for evidence.

Latest user issue: second exit seems unusable. Added explicit front-side test placement, crosshair and actual-ray/body-fit guidance. See docs/qa/portal-guide-review.md. One-sided policy unchanged; exact user pose unknown. Try the new button if reproducing.

Next lead task: investigate silhouette refusal bands/near-surface correctness.
Thin magenta outlines remain around some balls, honestly unresolved. Build a
connected GPU grazing corpus and estimate error through transit/ball root solve;
never erase unresolved or lower guards solely to beautify the image. Half-orbit
focusing can make an aperture fill most of the view: checker region provenance
may be FLAT even while camera is spherical; that is not an S3 chart boundary.

Claude can implement a bounded ray-diagnostic fixture or editor import/UI once
lead gives packet/query constraints. Muse can construct deterministic near-rim
and ball-silhouette rays through one/two portals, save seeds and report CPU/GPU
status and error; no shader repairs or tolerance choices. Prefer that narrow
corpus over more prose audits. Existing model tests cover carried-up camera and
halts; lead reran clamp/carry/budget fail-demos. No need repeat old assignments.

Later: connect coverage-aware authoring charts to editor transactions, save/load
controls, intrinsic measurements. Current preview is playable fixture, not an
editor. Godot is a future host, not a substitute for these query contracts.
Host-probe once each session, use browser queue; current worker50244. Full Node
result recorded in review. Push user-facing work and verify actual Pages assets.
