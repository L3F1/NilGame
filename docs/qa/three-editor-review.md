# Three-geometry editor exposure, 2026-09-12

Base b28e32b; reviewed on LeoPC, Windows, Node24.20.0. The existing connected
editor now has a World selector and Worlds-menu link for E3 / complete S3 / H3.
Open tools/connected-global-preview.html?preset=three. Default remains E3/S3/E3.
Opening a preset explicitly discards unsaved session edits and restarts the page;
this does not silently extend the model's transactional load to new region sets.

The host passes experimentalH3 to both model and renderer. Candidate edit
installation still uses renderer.replaceWorld; existing compile-first rollback,
history, pose preservation and GPU subset limits remain unchanged. H3 is
bounded R8/extent12 and supports the current ball/aperture envelope only.

## Checks rerun

- `node tools/check-queue.js page-check --three-geometry`: 6 checks passed on
  RTX5070Ti / ANGLE D3D11, cold profile. Approximate boot-to-ready wall time
  15127ms, not GPU frame time. Actual motion flat/sphere/hyperbolic and return;
  H3 ball radius .7 through form changes image; GPU hits agree with CPU owner
  and distance within .001; undo/redo, real download and file-input load retain
  document and pose; radius1.1 refused without installing a candidate.
- Same command with `--sw`: 6 passed, SwiftShader, cold profile; ready639ms.
  Different compilation times do not establish interactive rendering throughput.
- Existing `page-check --connected-global`: 52 passed, real GPU.
- Meaningful negative check: temporarily remove H3 opt-in from the host. New
  browser command exits1 with `Region runtime does not support h3`, zero checks.
  Restore integration and rerun both GPU backends successfully. No kernel repair
  or weakened CPU/GPU tolerance used.

Browser checks run through host queue. Worker restarted to load the new allowed
flag; confirmed old process was exactly node tools/check-queue.js --serve.
GPU evidence packets use separate three-editor-real/sw filenames so they do
not overwrite the original connected-global packet. No driver frame-time
distribution is claimed by this small editor acceptance probe.

## Images and limits

Inspected generated page-check-shot-three-h3-arrival.png and
page-check-shot-three-e3-return.png at 320x240. H3 arrival shows a clean green
ball landmark; the return view faces empty checker coverage. The latter is not
evidence of rich architecture. These images are regenerated by the command;
root PNGs remain ignored. Existing purple-patch investigations remain open.

The probe tests saved preset opening by URL, not automated click-navigation of
the selector. Broad H3 portal creation/removal/orientation combinations remain
future corpus work; the existing E3/S3 editor checks cover that shared UI.
This is a visible experimental editing milestone, not the polished-level gate
for a Godot host trial. Next: architecture/scale cues and GPU artifact diagnosis.

Full approved-host Node validation: node tools/test.js, 126/126 suites passed
(.agent-bridge/three-editor-suite.log).
